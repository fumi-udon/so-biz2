<?php

return [

    /*
    |--------------------------------------------------------------------------
    | KDS 端末 PIN → 店舗 ID（暫定: 環境変数 / 設定のみ。将来 DB へ移行可）
    |--------------------------------------------------------------------------
    |
    | JSON オブジェクト: "4桁のPIN文字列" => shop_id（整数）
    | 例: {"1234":1,"5678":2}
    |
    | 本番では .env の KDS_PIN_MAP に設定し、config キャッシュ後は .env の変更は反映されない。
    |
    */
    'pin_to_shop' => json_decode((string) env('KDS_PIN_MAP', '[]'), true) ?: [],

];
