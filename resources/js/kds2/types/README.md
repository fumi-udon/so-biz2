# types/

JSDoc / TypeScript 型定義置き場。

## 主な型（Phase 2 以降で定義予定）
- `Ticket`       — spec.md §4 GET /kds2/api/tickets の ticket オブジェクト
- `Batch`        — バッチ + テーブルのまとまり
- `PendingAction` — キュー内アクション要素
- `MasterData`   — GET /kds2/api/master レスポンス
